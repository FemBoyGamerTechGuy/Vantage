# Distro packaging lives in packaging/<distro>/ and is driven by the
# ./build entry point (`./build packages arch`). Packaging metadata is
# not installed into the staged tree — the packagers consume the source
# repository directly.
